const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;

// This configuration file is used to set up PostCSS with the Tailwind CSS plugin. The `plugins` object specifies that the `@tailwindcss/postcss` plugin should be used, allowing you to utilize Tailwind CSS features in your project.